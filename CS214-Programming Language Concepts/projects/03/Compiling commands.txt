c++
-build:    g++ SourceFileName.cpp -Wall -o BinaryFileName

ada
-gnatmake circle_area

elisp
-xemacs &
	-Ctrl-x b for scratch buffer
	-Ctrl-j to test function
	-M-x [program/funct name]
	-Ctrl-x Ctrl-w saves
	-Ctrl-h f for help, then type name of function to documentation

ruby
-ruby [filename]

------------------------
def computeGrades(myAverage)
	case (myAverage/10)
		when 10 then 'F'
		when 9 then 'F'
		when 8 then 'B'
		when 7 then 'C'
		when 6 then 'D'
		else 'f'
	end		
end